# API base URL (Elysia backend). On the web this is read at build/runtime from the
# public env. Mirrors the mobile app's EXPO_PUBLIC_API_URL.
NEXT_PUBLIC_API_URL=https://universal-starter.onrender.com
