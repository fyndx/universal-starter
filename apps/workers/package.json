{
  "name": "@universal/workers",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile workers ./src/index.ts",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@universal/db": "workspace:*",
    "@universal/logger": "workspace:*",
    "@universal/queue-kit": "workspace:*",
    "@universal/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bullmq": "^5.58.4"
  },
  "peerDependencies": {
    "typescript": "catalog:"
  }
}
